<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
    />
    <link rel="stylesheet" href="/styles/index.css" />
    <title>Homepage</title>
  </head>
  <body>
    <header><%- include("../partials/header.ejs") %></header>
    <main>
      <%- include("../partials/sub-nav.ejs") %>
      <hr />
      <div class="product-list">
        <h2 class="list-name">Featured</h2>
        <div class="products-container">
          <% products.filter(product=>product.isFeatured===true).slice(0,5).forEach(product => { %>
          <%-include('../components/product-card.ejs',{product:product}) %>
          <%}); %>
        </div>
      </div>
      <hr>
      <hr />
      <div class="product-list">
        <h2 class="list-name">Laptop</h2>
        <div class="products-container">
          <% products.filter(product=>product.category.name==='laptop').slice(0,5).forEach(product => { %>
          <%-include('../components/product-card.ejs',{product:product}) %>
          <%}); %>
        </div>
        <div class="more-btn">
          <a href="">More</a>
        </div>
      </div>
      <hr>
      <div class="product-list">
        <h2 class="list-name">Keyboard</h2>
        <div class="products-container">
          <% products.filter(product=>product.category.name==='keyboard').slice(0,5).forEach(product => { %>
          <%-include('../components/product-card.ejs',{product:product}) %>
          <%}); %>
        </div>
        <div class="more-btn">
          <a href="">More</a>
        </div>
      </div>
      <hr />
      <div class="product-list">
        <h2 class="list-name">Mouse</h2>
        <div class="products-container">
          <% products.filter(product=>product.category.name==='mouse').slice(0,5).forEach(product => { %>
          <%-include('../components/product-card.ejs',{product:product}) %>
          <%}); %>
        </div>
        <div class="more-btn">
          <a href="">More</a>
        </div>
      </div>
      <hr />
      <div class="product-list">
        <h2 class="list-name">Srceen</h2>
        <div class="products-container">
          <% products.filter(product=>product.category.name==='screen').slice(0,5).forEach(product => { %>
          <%-include('../components/product-card.ejs',{product:product}) %>
          <%}); %>
        </div>
        <div class="more-btn">
          <a href="">More</a>
        </div>
      </div>
      <hr />
      <div class="product-list">
        <h2 class="list-name">Headsphone</h2>
        <div class="products-container">
          <% products.filter(product=>product.category.name==='headsphone').slice(0,5).forEach(product => { %>
          <%-include('../components/product-card.ejs',{product:product}) %>
          <%}); %>
        </div>
        <div class="more-btn">
          <a href="">More</a>
        </div>
      </div>
      <hr />
    </main>

    <!-- footer -->
    <%- include("../partials/footer.ejs") %>
  </body>
  <script src="/scripts/product-card.js"></script>
</html>
